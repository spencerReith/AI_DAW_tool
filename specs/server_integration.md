# 'App' and 'Hum' Server Integration

## Background
Right now, there is a seperate server to run the audio generation through stable-audio (call app.py in backend), and a seperate server to run the generation thru musicGen (called hum.py in backend).

Our goal is to only have one server. We are not going to be making any changes to the frontend right now.